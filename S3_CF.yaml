AWSTemplateFormatVersion: 2010-09-09
Description: Create a S3 bucket with version enabled and SSE enabled
Resources:
  S3Bucket:
    Type: AWS::S3::Bucket
    Properties:
      BucketName: rawdatabucket
      BucketEncryption:
        ServerSideEncryptionConfiguration:
          - ServerSideEncryptionByDefault:
              SSEAlgorithm: AES256
          

